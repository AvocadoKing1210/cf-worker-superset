{
  "name": "execute-sql",
  "version": "1.0.0",
  "description": "Cloudflare Worker for SQL execution",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env=\"\"",
    "deploy:prod": "wrangler deploy --env=production",
    "type-check": "tsc --noEmit",
    "test:e2e": "tsx tests/runner.ts"
  },
  "devDependencies": {
    "wrangler": "^4.42.2",
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.3.3",
    "tsx": "^4.7.0"
  }
}
